
// Global test setup
beforeAll(async () => {
  // Any global setup can go here
});

afterAll(async () => {
  // Global cleanup
});

// Set longer timeout for database operations
jest.setTimeout(30000);
