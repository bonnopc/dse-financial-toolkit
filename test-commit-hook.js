// Test file with intentional formatting issues to test pre-commit hooks
const testFunction = () => {
  console.log('This has bad formatting');
  const obj = { a: 1, b: 2 };
  return obj;
};

export default testFunction;
